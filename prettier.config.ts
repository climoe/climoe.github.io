import { type Config } from "prettier";

const config: Config = {
  trailingComma: "none",
  semi: true,
  singleQuote: true,
  tabWidth: 1,
  useTabs: true,
  printWidth: 120
};

export default config;